# Server 模块局部指南

本目录包含 SQLite、仓储、服务和对象存储实现。依赖方向应保持为 API → service → domain/repository/storage。

## 事务与错误

- 仓储层封装 SQL，不向页面暴露数据库连接。
- 服务层编排事务和外部副作用。
- OSS 删除失败必须持久化重试任务。
- 外部 SDK 错误转换为稳定领域或 HTTP 错误。

修改服务流程时补集成测试，并更新对应核心概念和排障文档。
