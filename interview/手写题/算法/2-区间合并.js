function mearge(arr) {
    if (arr.length <= 1) return arr
    arr.sort((a,b) => a[0] - b[0]) // 按照start排序，从小到大

    const result = [arr[0]]

    for (let i = 1; i  < arr.length ; i++) {
        
    }

}