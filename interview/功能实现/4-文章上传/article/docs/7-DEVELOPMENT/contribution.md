# 贡献指南

- 使用 pnpm 和仓库现有脚本。
- 输入边界使用 schema 校验，错误必须显式处理。
- 保持不可变数据约定，避免跨层直接修改对象。
- 新行为先补测试，再修改实现。
- 不提交 `.env.local`、密钥、生成产物或本地数据库。
- 代码变更同步影响范围内的用户文档和参考文档。

提交前至少运行 `pnpm typecheck`、`pnpm lint` 和相关测试。
