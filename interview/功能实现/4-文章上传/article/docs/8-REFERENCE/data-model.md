# 数据模型参考

## Article

包含标题、摘要、标签、Tiptap JSON 正文、封面、草稿/发布状态和 revision。对外稳定字段以 API 响应为准。

## Asset 与上传记录

记录对象 key、MIME、大小、状态、文章引用和上传完成信息。内部字段不应在没有契约说明时作为公共 API 使用。

## 清理任务

记录待删除对象、重试次数、下次执行时间和最后错误，用于 OSS 删除失败后的持久化重试。

SQLite 表结构和索引属于实现细节；schema 变化必须先备份并更新迁移说明。
