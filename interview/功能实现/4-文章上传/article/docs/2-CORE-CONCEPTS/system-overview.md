# 系统总体架构

## 分层

```text
页面与组件
  -> Next.js API 路由
  -> 服务层（文章、上传、清理）
  -> 领域规则与富文本策略
  -> SQLite 仓储 / ObjectStorage 抽象
  -> 阿里云 OSS 或 fake OSS
```

页面负责交互和状态展示，API 负责边界校验和统一响应，服务层编排业务流程，领域层保存可测试的不变量，仓储和存储提供者负责外部持久化。

扩展新存储提供者时，不应让页面或领域层直接依赖 `ali-oss`。
