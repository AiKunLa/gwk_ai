const name = "John";
const age = 30;
const increment = `My dog ${name} is ${age} years old.`;

console.log(increment); // My dog John is 30 years old.
function getGreeting(name) {
    return `Hello, ${name}!`;
}