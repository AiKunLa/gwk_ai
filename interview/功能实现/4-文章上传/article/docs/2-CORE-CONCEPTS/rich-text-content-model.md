# 富文本内容模型

正文的唯一持久化来源是 Tiptap JSON，不直接把 HTML 作为主数据保存。

## 数据流

1. 编辑器维护 Tiptap JSON。
2. 保存接口校验结构和文章规则。
3. 服务端对渲染内容执行白名单清洗。
4. 详情和预览页面使用统一渲染器输出 HTML。

图片节点包含对象 key、替代文本、说明、对齐和尺寸等属性。新增节点或属性必须同步编辑器扩展、服务端规则、渲染器和测试。
