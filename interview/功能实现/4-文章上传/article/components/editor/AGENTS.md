# 编辑器局部指南

本目录负责 Tiptap 编辑器、工具栏、图片节点检查器、上传客户端和文章编辑状态。

## 关键约束

- 正文持久化格式是 Tiptap JSON。
- 图片上传必须有状态、进度、失败反馈和上传锁。
- 自动保存约每 15 秒触发，保存请求必须携带 revision。
- 不要只在客户端放宽节点属性；服务端规则和渲染器必须同步。
- 组件测试覆盖编辑、上传、冲突和错误状态。

扩展节点或上传行为时阅读 `docs/7-DEVELOPMENT/extending-tiptap-editor.md`。
